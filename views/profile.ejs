<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-gradient-to-br from-blue-100 via-purple-50 to-pink-100 flex flex-col items-center justify-center p-0">
    <!-- Header -->
    <header class="w-full bg-white/80 shadow-lg py-4 px-8 flex items-center justify-between fixed top-0 left-0 z-10">
      <div class="flex items-center gap-3">
        <img src="https://cdn-icons-png.flaticon.com/512/1828/1828884.png" alt="blog avatar" class="w-12 h-12 rounded-full shadow border-2 border-blue-300 bg-white">
        <span class="text-xl font-bold text-blue-700">MySocial</span>
      </div>
      <a href="/logout" class="bg-gradient-to-r from-red-400 via-pink-300 to-yellow-300 rounded-md px-4 py-2 text-sm font-bold shadow hover:scale-105 transition text-gray-800">Logout</a>
    </header>
    <!-- Main Card -->
    <main class="w-full max-w-2xl mt-24 mb-10">
      <div class="bg-white/90 rounded-2xl shadow-2xl p-8 flex flex-col items-center">
        <img src="https://cdn-icons-png.flaticon.com/512/1828/1828884.png" alt="blog avatar" class="w-24 h-24 rounded-full shadow border-4 border-blue-300 bg-white mb-4">
        <h3 class="text-3xl font-bold text-blue-700 mb-2">Hello, <%= user.name %>!</h3>
        <h5 class="mb-6 text-lg text-gray-600">Welcome back! Ready to share your thoughts?</h5>
        <form action="/posts" method="POST" class="w-full flex flex-col items-center gap-3">
          <textarea placeholder="What's on your mind..?" name="content" class="block p-3 outline-none w-full resize-none bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50 border-2 border-blue-200 text-gray-800 rounded-md placeholder:text-blue-400 shadow-lg"></textarea>
          <input type="submit" value="Create Post" class="px-6 py-2 bg-gradient-to-r from-blue-400 via-purple-300 to-pink-300 rounded-md cursor-pointer hover:scale-105 transition text-base font-bold text-white shadow-lg">
        </form>
        <div class="posts w-full mt-12">
          <h3 class="text-blue-500 text-xl font-semibold mb-4">Your Posts</h3>
          <div class="postcontainer flex flex-col gap-6">
            <% if (user.posts.length === 0) { %>
              <div class="text-center text-gray-400">No posts yet. Start sharing your thoughts!</div>
            <% } %>
            <% user.posts.forEach(post => { %>
              <div class="post w-full mb-2 p-6 border-2 border-blue-200 text-gray-800 rounded-xl bg-gradient-to-r from-blue-50 via-purple-50 to-pink-50 shadow-lg">
                <h4 class="text-blue-500 mb-2 font-bold"><%= user.username %></h4>
                <p class="text-base tracking-tight text-gray-700 mb-3"><%= post.content %></p>
                <div class="btns flex gap-4">
                  <a class="text-yellow-500 font-bold hover:text-blue-500 transition" href="/posts/<%= post._id %>/edit">Edit</a>
                  <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                    <button type="submit" class="text-red-500 font-bold hover:text-pink-500 transition">Delete</button>
                  </form>
                </div>
              </div>
            <% }) %>
          </div>
        </div>
      </div>
    </main>
  </div>
  </div>
</body>
</html>
