<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
     <div class="w-full min-h-screen bg-gradient-to-br from-blue-100 via-purple-50 to-pink-100 flex flex-col items-center justify-center p-0">
        <!-- Header -->
        <header class="w-full bg-white/80 shadow-lg py-4 px-8 flex items-center justify-between fixed top-0 left-0 z-10">
          <div class="flex items-center gap-3">
            <img src="https://cdn-icons-png.flaticon.com/512/1828/1828884.png" alt="blog avatar" class="w-12 h-12 rounded-full shadow border-2 border-blue-300 bg-white">
            <span class="text-xl font-bold text-blue-700">MySocial</span>
          </div>
          <a href="/login" class="bg-gradient-to-r from-blue-400 via-purple-300 to-pink-300 rounded-md px-5 py-2 text-sm font-bold shadow hover:scale-105 transition text-white">Login</a>
        </header>
        <!-- Main Card -->
        <main class="w-full max-w-xl mt-24 mb-10">
          <div class="bg-white/90 rounded-2xl shadow-2xl p-8 flex flex-col items-center">
            <img src="https://cdn-icons-png.flaticon.com/512/1828/1828884.png" alt="blog avatar" class="w-24 h-24 rounded-full shadow border-4 border-blue-300 bg-white mb-4">
            <h3 class="text-3xl font-bold text-blue-700 mb-2">Create Account</h3>
            <form action="/register" method="post" class="w-full flex flex-col items-center gap-4 mt-4">
              <input type="text" class="px-4 py-3 outline-none bg-gradient-to-r from-blue-50 via-purple-50 to-pink-50 border-2 border-blue-200 text-gray-800 rounded-md w-full placeholder:text-blue-400 shadow" name="name" placeholder="Name" required>
              <input type="text" class="px-4 py-3 outline-none bg-gradient-to-r from-blue-50 via-purple-50 to-pink-50 border-2 border-blue-200 text-gray-800 rounded-md w-full placeholder:text-blue-400 shadow" name="username" placeholder="Username" required>
              <input type="number" class="px-4 py-3 outline-none bg-gradient-to-r from-blue-50 via-purple-50 to-pink-50 border-2 border-blue-200 text-gray-800 rounded-md w-full placeholder:text-blue-400 shadow" name="age" placeholder="Age" required>
              <input type="email" class="px-4 py-3 outline-none bg-gradient-to-r from-blue-50 via-purple-50 to-pink-50 border-2 border-blue-200 text-gray-800 rounded-md w-full placeholder:text-blue-400 shadow" name="email" placeholder="Email" required>
              <input type="password" class="px-4 py-3 outline-none bg-gradient-to-r from-blue-50 via-purple-50 to-pink-50 border-2 border-blue-200 text-gray-800 rounded-md w-full placeholder:text-blue-400 shadow" name="password" placeholder="Password" required>
              <input type="submit" class="px-6 py-2 bg-gradient-to-r from-blue-400 via-purple-300 to-pink-300 rounded-md cursor-pointer hover:scale-105 transition text-base font-bold text-white shadow-lg w-full" value="Create">
            </form>
          </div>
        </main>
     </div>
</body>
</html>